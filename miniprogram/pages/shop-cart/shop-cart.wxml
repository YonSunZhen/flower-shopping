<import src="../../template/shop-cart-template/shop-cart-template.wxml"/>

<view class="container">
    <template is="{{goodsList.length > 0 && isLogin  ? 'cart-goods-list' : 'no-goods'}}" data="{{goodsList,isLogin,openid,isDone,allSelect,totalPrice}}"></template>
</view>
